// let arrayCounter = function(arr) {
//     return `Array length ${arr.length}`
// }

// module.exports.multiply = function(x, y) {
//     return `${x} * ${y} = ${x * y}`
// } 

// module.exports.someValue = 228

let arrayCounter = function(arr) {
    return `Array length ${arr.length}`
}

let multiply = function(x, y) {
    return `${x} * ${y} = ${x * y}`
}

let someValue = 228

module.exports = { arrayCounter, multiply, someValue }